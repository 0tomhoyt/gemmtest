# AI 辅助开发流程经验：UniGEMM Fuzz Test 案例

## 项目背景

用 AI（Claude Code）从零开发了一个多线程模糊测试框架，用于验证 BLAS GEMM 实现的正确性。项目包含 C/C++17 代码、CMake 构建系统、5 种精度类型的测试支持，最终形态约 30 个测试阶段、47 次 commit。整个开发过程约 1-2 周。

---

## 开发流程总览

```
需求讨论 → 框架搭建 → 功能迭代 → 细节打磨 → 反复验证
```

### 第一阶段：框架搭建（约 10 次 commit）

**做了什么**：让 AI 根据需求描述生成初始代码框架，包括线程管理、随机参数生成、矩阵比较、报告输出等模块。

**经验**：
- **AI 适合做"从 0 到 1"的脚手架工作**。一次对话就能生成一个可编译运行的完整框架，省去了大量前期搭建时间。
- **但初始设计往往过于理想化**。比如 AI 默认用随机线程数、随机 BLAS 线程数，实际运行时才发现线程超额订阅问题。需要在真实环境中尽早验证。
- **关键决策要人工把关**。比如独立参考实现（openblas.c）的设计思路是人提出的——故意用不同的代码结构避免相关性 bug，AI 负责实现细节。

### 第二阶段：功能扩展（约 20 次 commit）

**做了什么**：逐步增加精度类型（SGEMM → SHGEMM → SBGEMM → HGEMM → BGEMM），从 6 阶段扩展到 30 阶段，增加进度条、阶段报告、失败详情等。

**经验**：
- **增量式开发效果好**。每次对话聚焦一个具体功能（"加 SHGEMM 支持"、"加进度条"），AI 能快速完成。避免一次性要求太大范围的改动。
- **AI 擅长模式复制**。新增一个精度类型的测试路径，本质上是复制已有模式并修改细节。AI 做这类工作非常高效且不易出错。
- **但容易忽略边界情况**。比如半精度数据生成策略改了三轮：先是 float→half 截断（不对），再是随机 bit pattern（可读性差），最后才稳定为 native InitMatrix + expand。这说明 AI 在涉及数值精度的领域需要人来判断正确性。

### 第三阶段：细节打磨（约 15 次 commit）

**做了什么**：修复输出交织、统一代码风格、优化内存分配、调整容差值、增强错误报告等。

**经验**：
- **并发 bug 是 AI 的弱项**。进度条与错误输出交织的问题，本质是 stdout/stderr 共享终端光标的竞态。AI 最初尝试用 stderr 隔离（注释还写着"avoids mixing with progress bar"），实际上并不能解决问题。最终需要人指出症状，AI 才加上 mutex 同步。
- **代码风格问题容易被 AI 忽略**。指针声明风格混用（`*T` vs `T*`）、文件命名错误（.cpp 应为 .h）这类问题，AI 在生成代码时不会主动统一，需要后期专门清理。
- **验证驱动修复很有效**。很多问题是在运行 `test_buggy.sh`（故意有 bug 的实现）时暴露的。AI 生成的代码"能编译"不代表"能正确工作"，必须跑真实测试。

---

## 核心经验

### 1. 让 AI 做"重活"，人做"判断"

| AI 擅长 | 人擅长 |
|---------|--------|
| 生成模板代码、复制模式 | 判断数值精度方案是否正确 |
| 重构、重命名、批量修改 | 发现并发/竞态等微妙 bug |
| 写 commit message、文档 | 决定架构方向（阶段结构、线程模型） |
| 一次性修改多个文件 | 判断容差值是否合理 |

### 2. 小步迭代，频繁验证

最有效的开发节奏是：
```
AI 生成/修改 → 编译 → 跑测试 → 看结果 → 发现问题 → 让 AI 修
```

每次 commit 对应一个小的、可验证的改动。不要积攒大量改动再测试。

### 3. 用"反面测试"暴露问题

`test_buggy.sh`（故意有 bug 的实现）是整个项目最有价值的工具之一。它让 AI 生成的测试框架自身得到验证——如果连故意注入的 bug 都检测不到，框架就是废的。很多输出格式、错误报告的问题都是通过这个测试发现的。

### 4. AI 生成的注释和文档不一定准确

有个典型例子：worker 里打印错误信息的代码，AI 写了注释 `"Print failure parameters to stderr (avoids mixing with progress bar)"`，但实际上 stderr 和 stdout 共享终端光标，并不能避免混合。**不要盲信 AI 写的注释，要验证其正确性。**

### 5. 重构可以放心交给 AI

项目中多次大规模重构（文件重命名、函数重命名、指针风格统一、数据生成策略重写），AI 都能正确完成且不遗漏。这类"机械性但需要细心"的工作是 AI 的强项。

### 6. 保留完整的 commit 历史

47 次 commit 完整记录了每次问题的发现和修复过程。这不仅方便回溯，也为后续复盘提供了素材。AI 可以根据 commit 历史生成总结文档（比如本文），形成知识沉淀。

---

## 开发效率对比（主观估计）

| 任务类型 | 纯人工估计耗时 | AI 辅助实际耗时 | 提效倍数 |
|---------|--------------|----------------|---------|
| 初始框架搭建 | 2-3 天 | 2-3 小时 | ~8x |
| 新增精度类型支持 | 1 天/种 | 1-2 小时/种 | ~5x |
| 输出格式调整 | 0.5 天 | 30 分钟 | ~8x |
| 并发 bug 修复 | 0.5 天 | 1 小时（含排查） | ~4x |
| 代码风格统一 | 0.5 天 | 15 分钟 | ~16x |
| 写文档/README | 0.5 天 | 10 分钟 | ~24x |

最大的效率提升在"重复性但需要细心"的工作上（重构、文档、模式复制）。涉及领域知识判断的工作（数值精度、并发设计）仍需人工主导。

---

## 适合 AI 辅助开发的项目特征

这个项目之所以适合 AI 辅助，有几个前提：

1. **需求明确**——GEMM 正确性测试是一个定义清晰的问题，不容易跑偏。
2. **模块化强**——线程管理、数据生成、矩阵比较、报告输出各自独立，AI 可以逐模块处理。
3. **有明确的验证标准**——编译通过 + 测试通过 + `test_buggy.sh` 能检出 bug，三个层次的验证都很明确。
4. **代码量适中**——单个文件几百行，AI 的上下文窗口完全够用。

如果项目需求模糊、验证困难、或者单文件超长，AI 辅助的效果会打折扣。
